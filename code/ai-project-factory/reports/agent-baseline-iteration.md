# Agent资料驱动项目与员工迭代

日期：2026-09-07。项目入口：http://127.0.0.1:5173/#/projects/5f7ffce47f1c4f67b1c4048324bb1695/evidence

## 已核实资料

Agent项目/readme.txt 中代码路径可读；飞书目录、需求疑问表、输入输出表在已登录浏览器可访问。目录6份PDF和1份XLSX均打开预览。PDF下载按钮不可用，因此通过正常页面读取节选，没有宣称全文导入。

感知表读取字段样例，模型表读取行2–10、执行表行2–5、待办表行2–10。QA仅部分页/行。来源逐项保存到平台并标记excerpt。源码203个选定文件已建立不可变快照，排除隐藏目录、配置和构建产物；不是整个代码目录的完整备份。源码清单与内容哈希可在平台核查。

## 项目目标与任务

资料基线 → 需求分析 → 独立需求评审 → 架构设计 → 独立架构评审 → 隔离开发 → 全模块单元测试 → E2E → 部署前评审 → 本地部署验证 → 最终质量评审 → 人类验收。修订9已增加提前范围裁定节点，共13节点，7条显式疑问，保存快照b6d3c74eaec64d65b4c0404a1a56f2e0。

需求产物：requirements.md、perception-contract.json、execution-contract.json、model-requirements.md、open-questions.md、traceability.csv。评价关注行为、字段、协议和证据对齐，不能用文字相似度或代码复制量替代验收。

6个AI岗位：需求、架构、开发、测试、部署、独立质量；1个人类负责人。修改触发新运行，保留旧证据，上游变化使相关下游失效；实际DAG执行和缓存复用引擎尚未接入，这些目前是项目设计规则。

## 本轮平台扩展

- 资料基线、来源范围和哈希保存；模型上下文显式标注节选和截断。
- 12类独立设计评估，禁止将未运行行为或不完整资料判通过；输入绑定方案和员工版本。
- 评估结果带回项目对话优化；历史版本明确标识。
- 需求员工文档试运行，六文件严格校验、JSON语法检查、文件哈希及持久化；阻塞可以伴随可核查的部分产物。
- Gradle基线运行副本、CRLF转换证据、已有发行包缓存、离线/下载模式、日志/退出码、超时清理。
- 可取消运行；服务重启保留中断状态；前端全宽页面保持项目入口。

Codex CLI使用本机登录、临时只读工作目录及结构化返回；可用CODEX_REASONING_EFFORT配置推理强度，默认medium。此参数见[官方配置文档](https://learn.chatgpt.com/docs/config-file/config-reference)。未证明首次超时由推理强度造成，首轮过大输入和输出时延仍需持续观察。

## 已完成的首次独立评估

评估995598711d8845ee9b2f491076035b87：4项fail（需求疑问、单元规格、E2E规格、员工评测），6项blocked（资料、感知、执行、模型、架构、部署），1项pass（仅重跑设计规则），1项not_assessed（真实行为）。

据此已通过平台员工接口补充全部6位员工的evaluations/cases.json、contracts/handoff.json；测试员工新增evaluations/e2e-matrix.json，含36格未执行候选规格；模型继续迭代方案。样例本身不是已通过的测试结果。

## 基线执行证据

1. 92e48bec1d9f4adabc3b4ea591745f4f：退出码2，gradlew CRLF无法启动。
2. 118b27c955d641928d242a379ef68805：转换副本换行后120秒超时，原始源码保留。
3. f257d25324754069b799e55601402eb2：复用Gradle8.9发行包后实际启动构建，退出码1，离线缺Kotlin2.3.0插件；未到业务测试执行。

## 边界

当前已运行的是平台方案生成、独立设计评估、隔离基线命令；需求试运行及后续迭代结果以平台最新记录为准。完整车辆Agent的自动开发、E2E及部署尚未完成。未连接真实车辆，未发布制品。设计ready不等于实施、测试或部署完成。


## 后续定位与验证

4. dca12c82b14c4fb0958611b13f540dec：通过平台UI允许下载依赖后，54秒内进入agent-kernel配置，但构建脚本强制要求Nexus发布凭据而退出1。没有运行任何车辆业务测试。问题已进入项目Q07，需将测试配置与发布凭据解耦。

平台回归：33项后端测试、27项前端测试通过；生产构建通过（主包大于500KB警告仍存在）。覆盖输入快照、范围限制、原始源码不变、CRLF转换、需求产物路径/JSON校验、取消与重试、疑问数量、历史版本、错误输入保留、依赖下载选项和产物展开。

浏览器实际验证：项目方案13节点、7条疑问、员工评测文件可打开；资料与评估页面可启动独立评估和联网基线测试。补充了后台更新同步，避免自动化操作后界面仍停留旧版本。

## 需求员工实际产物

运行eba8484401aa4ce1a209d6009e2e4503，方案修订9、需求员工版本2。6份文档已实际保存于`.data/analyses/eba8484401aa4ce1a209d6009e2e4503/`，逐文件核对SHA-256与数据库一致，两份契约JSON可解析。

产物包含六场景需求、6项已读感知字段、4类执行动作、模型能力支持/未知状态、Q01–Q07疑问和14行追溯记录。仍为blocked部分草稿，未有完整资料或已批准决策，不能作为完整需求验收通过。

员工输出说明“未写入磁盘”指模型未调用写文件工具；平台实际完成持久化。界面已明确区分模型说明与平台存储状态。平台计算run-manifest.json，绑定项目/员工/资料版本及真实输出哈希，不让模型臆造运行元数据。

第二轮独立评估c4e4536210124bd289fac965c44d5f66已基于修订9、员工改进文件、需求产物和基线失败日志发起；以平台最终记录为准。

## 第二轮最终结论

{"blocked": 6, "fail": 4, "pass": 1, "not_assessed": 1}。评分分布尚未改善，不能宣称员工或业务验收通过。已从岗位描述推进到真实部分需求产物、具体缺口与可追踪的验证记录。

评估称实际14节点的建议与平台13节点不符，已人工核对并在平台资料中保存校准记录38ffed55402c4a25bbab8063c5006eed。评估原文保留，没有据此篡改评分。

后续优先级：测试/发布凭据解耦；需求运行带齐平台元数据和既有日志；全模块行为测试清单与逐用例E2E夹具；批准契约后接入架构/开发执行与DAG阶段重跑。现阶段不得宣称全链路已跑通。
