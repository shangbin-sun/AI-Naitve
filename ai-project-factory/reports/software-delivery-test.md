# 软件交付团队设计集成测试

日期：2026-09-07。范围：团队设计阶段，创建需求分析、架构设计、开发、独立测试、部署及失败恢复的完整方案。

## 实际结果

- 真实 Codex 生成耗时约 3 分钟，生成 5 位 AI 员工、1 位人类负责人、18 个流程节点。
- 方案包括最多 3 轮总尝试、测试失败修复与复测、部署失败回滚及恢复验证、恢复失败或重试超限转人工。
- 每阶段有负责人、输入、输出路径和验收条件；开发与独立测试分别归属不同员工。
- 16 项只读集成检查全部通过，5 位员工的 ZIP 工程可导出且配置与方案一致。
- 项目快照创建成功，重复请求返回同一项目，快照保存全部团队和员工工程。
- 后端原有 15 项测试通过；新增 6 项完整性测试先失败，修复后共 21 项通过。
- 服务重启加载修复后，真实 API 拒绝空验收条件，未改变已有设计版本。

团队设计 ID：a9371cefd9fa49ebbc92d1b980bf2d27。
项目快照 ID：88e32a801fd44ae6bc56e763f53e45b6。

## 修复

此前缺少员工工作指令、输入、输出或步骤验收条件的方案也能标记 ready。现在 ready 方案必须满足上述内容完整性；未就绪草稿仍可逐步完善。

复验命令：

```bash
backend/.venv/bin/pytest backend/tests -q
backend/.venv/bin/python scripts/check_design.py a9371cefd9fa49ebbc92d1b980bf2d27
```

## 不能宣称已通过的部分

本次没有实际生成待办应用源码、运行业务测试或部署待办应用。现有平台尚缺员工执行器、条件分支调度、隔离工作区、运行资源绑定及人工干预执行状态。

当前返工与跳过规则存储在验收说明中，尚不是可执行的结构化条件。后续应将条件、终态、重试上限和升级处理建模，再接员工执行，分别验证成功路径、测试失败路径和部署失败回滚路径。不能把节点存在或设计检查通过等同于端到端交付成功。
